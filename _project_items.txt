{"items":[{"content":{"body":"## Architecture: WhatsApp ↔ Claude Bridge\n\n### Overview\nA real-time bridge that connects WhatsApp (and eventually Telegram) to Claude, maintaining persistent conversation sessions per chat.\n\n### Current State (baseline)\n- **Go bridge**: connects to WhatsApp via WebSocket (whatsmeow library), stores messages in SQLite, exposes HTTP API on port 8080 for sending\n- **Python MCP server**: reads SQLite, exposes mcp__whatsapp__* tools to Claude desktop app\n- **Polling approach** (current): Claude scheduled task polls every 1 minute — being replaced with inline real-time handler\n\n### Target Architecture\n\nMessage flow:\n1. WhatsApp message arrives → Go bridge WebSocket handler\n2. Look up session_id for chat JID in sessions.json\n3. Spawn: `echo \"msg\" | claude -p --output-format json [--resume <session_id>]`\n4. Parse JSON: extract `result` (reply text) and `session_id`\n5. Save session_id to sessions.json\n6. POST reply to bridge HTTP API (port 8080) → sent to WhatsApp\n\n### Key Decisions\n\n**1. Inline handler vs. separate gateway**\nDecision: inline in Go bridge (simpler). Gateway to be extracted later when adding Telegram.\n\n**2. Session persistence approach**\nThree options evaluated:\n- Option A: Anthropic API + stored JSON history — requires separate API billing, rejected\n- Option B: persistent subprocess — process management complexity, rejected\n- Option C (chosen): CLI spawn-on-demand + --resume flag. Process exits after each reply. Claude Code persists session to disk. Resumed via session_id next call. No separate API account needed.\n\n**3. Claude CLI invocation**\n```\necho \"user message\" | claude -p --output-format json --resume <session_id>\n```\n- Prompt via stdin (avoids 3s stdin wait when passed as arg)\n- --output-format json for structured output\n- Key response fields: result (reply), session_id (persist for next call)\n- Status: tested and confirmed working ✅\n\n**4. Session registry**\n- File: sessions.json in bridge directory\n- Maps chat JID to Claude session ID\n- New chat: spawn without --resume, save returned session_id\n- Existing chat: spawn with --resume <session_id>\n\n**5. Message filtering**\n- Skip fromMe=true messages (avoid replying to own messages)\n- Spawn in goroutine (non-blocking)\n\n**6. Future multi-channel plan**\n- Add Telegram bot routing to same session logic\n- Extract to standalone gateway at that point\n- Codex routing: messages prefixed with \"codex\" routed to Codex MCP thread\n\n### Confirmed Working\n- Claude CLI --resume: tested with 2-call xaxe test, context fully preserved ✅\n- Claude CLI PATH: fixed (npm reinstall, version 2.1.154) ✅\n- Repo: https://github.com/RonenDotan/whatsapp-claude-bridge ✅\n\n### Next Steps\n1. Create branch: feature/claude-inline-handler\n2. Modify main.go: goroutine on message receive → spawn Claude CLI → send reply\n3. Implement sessions.json read/write\n4. End-to-end test\n\n### Architecture Diagram\n\n```\n┌──────────────────────────────────────────┐\n│            WhatsApp Servers              │\n└───────────────────┬──────────────────────┘\n                    │ WebSocket (whatsmeow)\n                    ▼\n┌──────────────────────────────────────────┐\n│           Go Bridge (main.go)            │\n│                                          │\n│  ┌─────────────┐   ┌──────────────────┐ │\n│  │  WebSocket  │──▶│ Message Handler  │ │\n│  │  Listener   │   │  (goroutine)     │ │\n│  └─────────────┘   │                  │ │\n│                    │ 1. Skip fromMe   │ │\n│  ┌─────────────┐   │ 2. Read          │ │\n│  │  SQLite DB  │   │    sessions.json │ │\n│  │ messages.db │   │ 3. Spawn claude  │ │\n│  └─────────────┘   │    CLI           │ │\n│                    │ 4. Parse reply   │ │\n│  ┌─────────────┐   │ 5. Save session  │ │\n│  │  HTTP API   │◀──│ 6. Send reply    │ │\n│  │  :8080      │   └──────────────────┘ │\n│  └──────┬──────┘                        │\n└─────────┼────────────────────────────── ┘\n          │ HTTP POST (send message)\n          ▼\n┌──────────────────────────────────────────┐\n│           WhatsApp Servers               │\n└──────────────────────────────────────────┘\n\n          Message Handler spawns:\n\n┌──────────────────────────────────────────┐\n│  echo \"msg\" | claude -p                  │\n│    --output-format json                  │\n│    --resume <session_id>                 │\n│                                          │\n│  Sessions stored by Claude Code at:      │\n│  ~/.claude/projects/<session_id>         │\n│                                          │\n│  sessions.json (bridge dir):             │\n│  { \"chat_jid\": \"session_id\", ... }       │\n└──────────────┬───────────────────────────┘\n               │ HTTPS\n               ▼\n┌──────────────────────────────────────────┐\n│         Anthropic API                    │\n│      (Claude Sonnet / Opus)              │\n└──────────────────────────────────────────┘\n```\n","number":1,"repository":"RonenDotan/whatsapp-claude-bridge","title":"Architecture: WhatsApp ↔ Claude Bridge","type":"Issue","url":"https://github.com/RonenDotan/whatsapp-claude-bridge/issues/1"},"id":"PVTI_lAHOAP5YJ84BZFlzzguGPSw","repository":"https://github.com/RonenDotan/whatsapp-claude-bridge","status":"In Progress","title":"Architecture: WhatsApp ↔ Claude Bridge"},{"content":{"body":"## Overview\nAdd support for WhatsApp voice notes by transcribing them locally using OpenAI Whisper CLI before passing to Claude/Codex.\n\n## Current State\n- Audio messages are detected by the Go bridge and stored in SQLite with media credentials\n- `downloadMedia()` pipeline exists and can retrieve the .ogg file from WhatsApp CDN\n- No transcription exists — voice notes are silently ignored by handleMessage() (no text content = skipped)\n\n## Decision: Local Whisper via subprocess\n\n**Approach chosen:** Shell out to `whisper` CLI per voice note.\n\n**Rejected alternatives:**\n- OpenAI Whisper API — small cost, avoided for simplicity\n- Embedded via whisper.cpp Go bindings — more complex CGo setup\n- Integrated into MCP server — would keep model in RAM permanently; bad tradeoff since voice notes are infrequent (99% of the time model sits idle)\n\n**Why subprocess wins:**\n- Whisper loads into memory only when a voice note arrives, transcribes, then exits\n- Zero memory footprint between voice notes\n- Simple: ~10 lines of Go using exec.Command\n- No persistent process or service to manage\n\n## Implementation Plan\n\n### Prerequisites\n```\npip install openai-whisper\n```\nffmpeg is already installed (used by send_audio_message).\n\n### Flow\n```\nIncoming WhatsApp message\n        |\n   AudioMessage detected\n        |\n   downloadMedia() → /tmp/audio_<id>.ogg\n        |\n   exec.Command(\"whisper\", file, \"--model\", \"base\", \"--output_format\", \"txt\")\n        |\n   Read transcript from output file\n        |\n   handleMessage() with \"[🎤 Voice]: \" + transcript\n        |\n   Claude / Codex as normal\n        |\n   sendWhatsAppMessage() → user\n```\n\n### Model\nUse `base` model:\n- ~150MB disk, ~150MB RAM (loaded only during transcription)\n- Handles Hebrew + English well\n- ~real-time speed on CPU for short voice notes\n\n### Changes to main.go\n- In `handleMessage()`: check for `mediaType == \"audio\"` \n- Call `downloadMedia()` to get the .ogg path\n- Run `whisper` subprocess, capture transcript\n- Inject as text and route to LLM normally\n- On whisper failure: send ⚠️ error message to user\n\n## Branch\n`feature/whisper-transcription` (branched from main after claude-inline-handler merge)\n\n---\n\n## Implementation & Fixes\n\n### Commits on `feature/issue-9-whisper-transcription`\n\n**feat: voice message transcription via local Whisper (#9)**\n- Added `transcribeAudio(filePath string) (string, error)` function\n- Runs `whisper <file> --model base --output_format txt --output_dir <tmpDir>` with 60s timeout\n- Reads .txt output, cleans up both .ogg and .txt temp files after\n- Hook added in `handleMessage()`: detects audio messages in whitelisted chats, downloads .ogg, transcribes, injects `[🎤 Voice]: <transcript>` and routes to Claude/Codex as normal\n- On failure: sends `⚠️ Could not transcribe voice message: <error>` to user\n\n**fix: transcription blocked by IsFromMe flag**\n- Root cause: `!msg.Info.IsFromMe` was `false` for voice notes sent from user's own phone (bridge receives them as outgoing echoes with `IsFromMe=true`)\n- Fix: replaced with `!isSentByUs(msg.Info.ID)` which only blocks messages the bridge itself sent, not messages from the user's phone\n\n**fix: remove invalid --language auto arg from whisper command**\n- `--language auto` is not a valid whisper argument (not in whisper's `LANGUAGES` dict) — argparse rejected it with exit code 2\n- Fix: removed the flag entirely (whisper auto-detects language by default)\n- Also added `PYTHONUTF8=1` env var to prevent cp1252 UnicodeEncodeError on Windows when whisper prints Hebrew/non-Latin characters\n- Switched `cmd.Run()` → `cmd.CombinedOutput()` so whisper error text surfaces in error messages instead of being silently swallowed\n\n### Status: ✅ Working\nManually verified: transcribed an existing .ogg voice note successfully. Voice messages sent to Claude/Codex WhatsApp channels are now transcribed and forwarded to the LLM.","number":9,"repository":"RonenDotan/whatsapp-claude-bridge","title":"Voice message transcription via local Whisper","type":"Issue","url":"https://github.com/RonenDotan/whatsapp-claude-bridge/issues/9"},"id":"PVTI_lAHOAP5YJ84BZFlzzguGTVo","repository":"https://github.com/RonenDotan/whatsapp-claude-bridge","status":"Closed","title":"Voice message transcription via local Whisper"},{"content":{"body":"## Prompt Caching\n\n### Overview\nAnthropic supports prompt caching natively. Repeated conversation prefixes can be cached server-side, reducing token costs significantly.\n\n### How It Works\n- On each API call, the full conversation history is sent to Anthropic\n- If the beginning of the conversation (the \"prefix\") matches a previously cached version, Anthropic reuses the cached KV state\n- Cached tokens cost ~90% less than regular input tokens\n- Cache TTL is typically 5 minutes (extended with cache_control: ephemeral) or up to 1 hour with explicit pinning\n\n### Key Clarification\nA common misconception: keeping a subprocess alive does NOT reduce token costs. LLMs are stateless — every API call re-processes all tokens from scratch regardless of local process state. The savings come from Anthropic's server-side cache, not from local process continuity.\n\n### Applicability to This Project\n- Our Option C (spawn-on-demand + resume) sends the full conversation history on each call\n- Earlier turns in long conversations are prime candidates for caching (same prefix, append-only)\n- Both Option A and Option C benefit equally from prompt caching\n\n### Implementation Approach\nTo explicitly use prompt caching with the Anthropic API:\n- Set `cache_control: {\"type\": \"ephemeral\"}` on the system prompt and earlier conversation turns\n- The Claude CLI may handle this automatically — needs investigation\n\n### Action Items\n- [ ] Investigate whether Claude CLI (`claude -p`) automatically uses prompt caching\n- [ ] If not, consider switching to direct API calls (with separate billing) to enable explicit caching\n- [ ] Measure actual token usage per conversation turn to quantify savings\n\n### References\n- https://docs.anthropic.com/en/docs/build-with-claude/prompt-caching\n\n### Related\n- #1 Architecture\n- Decision: Session Persistence Strategy\n","number":8,"repository":"RonenDotan/whatsapp-claude-bridge","title":"Prompt Caching","type":"Issue","url":"https://github.com/RonenDotan/whatsapp-claude-bridge/issues/8"},"id":"PVTI_lAHOAP5YJ84BZFlzzguGTWA","repository":"https://github.com/RonenDotan/whatsapp-claude-bridge","status":"Pending Closure","title":"Prompt Caching"},{"content":{"body":"## Multi-Channel Strategy\n\n### Goal\nSupport multiple messaging channels (WhatsApp, Telegram, others) all routing to the same persistent Claude session per conversation.\n\n### Current State\nWhatsApp only, inline handler in Go bridge.\n\n### Planned Expansion\n\n**Phase 1 (current):** WhatsApp inline handler\n**Phase 2:** Add Telegram\n- Add python-telegram-bot or similar Telegram bot\n- Routes to same session logic\n- At this point, extract shared logic to standalone gateway service\n\n**Phase 3 (future):** Additional channels\n- SMS, Slack, email, etc.\n\n### Session Namespace\nWhen multi-channel is active, prefix session keys with channel name:\n```json\n{\n  \"whatsapp:120363409956054412@g.us\": \"session_id_abc\",\n  \"telegram:123456789\": \"session_id_xyz\"\n}\n```\nSame Claude session can be shared across channels (one continuous conversation) or kept separate — user preference.\n\n### Routing Rules (future)\nBeyond channel routing, the gateway could route based on message content:\n- Message starts with \"codex\" → route to Codex MCP thread (using existing codex-router pattern)\n- Otherwise → route to Claude session\n\n### Related\n- #1 Architecture\n","number":7,"repository":"RonenDotan/whatsapp-claude-bridge","title":"Decision: Multi-Channel Strategy","type":"Issue","url":"https://github.com/RonenDotan/whatsapp-claude-bridge/issues/7"},"id":"PVTI_lAHOAP5YJ84BZFlzzguGTWc","repository":"https://github.com/RonenDotan/whatsapp-claude-bridge","status":"Closed","title":"Decision: Multi-Channel Strategy"},{"content":{"body":"## Message Filtering\n\n### Overview\nNot every incoming event from the WhatsApp WebSocket should trigger a Claude response. Filtering logic prevents loops and noise.\n\n### Current Filter Rules\n\n| Rule | Reason |\n|------|--------|\n| Skip `fromMe = true` | Avoids Claude replying to its own sent messages (loop prevention) |\n\n### Implementation\nCheck the message event's `Info.IsFromMe` field in the Go bridge before spawning Claude.\n\n```go\nif evt.Info.IsFromMe {\n    return // skip\n}\n```\n\n### Open Questions / Future Rules\n- **Group vs. DM**: Should Claude respond in all groups, or only specific ones? Currently targeting specific group JID.\n- **Media messages**: Audio, images, video — skip or transcribe? (see #Support Transcribe ticket)\n- **System messages**: Group join/leave events — skip these\n- **Rate limiting**: Should there be a cooldown per chat to avoid spamming?\n\n### Related\n- #1 Architecture\n- #Support Transcribe\n","number":6,"repository":"RonenDotan/whatsapp-claude-bridge","title":"Decision: Message Filtering","type":"Issue","url":"https://github.com/RonenDotan/whatsapp-claude-bridge/issues/6"},"id":"PVTI_lAHOAP5YJ84BZFlzzguGTW4","repository":"https://github.com/RonenDotan/whatsapp-claude-bridge","status":"Closed","title":"Decision: Message Filtering"},{"content":{"body":"## Session Registry Design\n\n### Overview\nA JSON file (`sessions.json`) in the bridge directory maps each WhatsApp chat JID to a Claude session ID.\n\n### Format\n```json\n{\n  \"120363409956054412@g.us\": \"39e9f5fb-98a3-4cbe-8a1b-58737d6bcc27\",\n  \"972501234567@s.whatsapp.net\": \"a1b2c3d4-...\",\n  \"another_group@g.us\": \"e5f6g7h8-...\"\n}\n```\n\n### Logic\n1. Message arrives with `chatJID`\n2. Read `sessions.json`\n3. If `chatJID` key exists → use `--resume <session_id>`\n4. If not → spawn without `--resume`, capture `session_id` from JSON response\n5. Write updated map back to `sessions.json`\n\n### Concurrency\n- Protect file reads/writes with a mutex (multiple goroutines may handle messages simultaneously)\n- Or serialize all Claude calls through a channel per JID\n\n### Edge Cases to Handle\n- `sessions.json` does not exist yet → create on first write\n- Session ID is stale (session expired or deleted) → detect error from CLI, start fresh session, update registry\n- Multiple messages from same chat arrive simultaneously → queue or mutex per JID\n\n### Future\n- When Telegram is added, extend with a prefix: `\"whatsapp:120363...\": \"session_id\"`, `\"telegram:123456\": \"session_id\"`\n\n### Related\n- #1 Architecture\n","number":5,"repository":"RonenDotan/whatsapp-claude-bridge","title":"Decision: Session Registry Design","type":"Issue","url":"https://github.com/RonenDotan/whatsapp-claude-bridge/issues/5"},"id":"PVTI_lAHOAP5YJ84BZFlzzguGTXU","repository":"https://github.com/RonenDotan/whatsapp-claude-bridge","status":"Closed","title":"Decision: Session Registry Design"},{"content":{"body":"## Claude CLI Invocation Format\n\n### Decision\n\n```bash\necho \"user message\" | claude -p --output-format json --resume <session_id>\n```\n\n### Key Flags\n\n| Flag | Purpose |\n|------|---------|\n| `-p` | Print mode (non-interactive, exits after response) |\n| `--output-format json` | Structured JSON output for easy parsing |\n| `--resume <session_id>` | Resume existing session by ID |\n| stdin (piped) | Pass prompt via stdin, not positional arg |\n\n### Why stdin instead of positional arg?\nWhen the prompt is passed as a CLI argument, the binary waits 3 seconds for stdin before proceeding. Piping via stdin avoids this delay.\n\n### JSON Response Format\n```json\n{\n  \"type\": \"result\",\n  \"subtype\": \"success\",\n  \"is_error\": false,\n  \"result\": \"The reply text here\",\n  \"session_id\": \"39e9f5fb-98a3-4cbe-8a1b-58737d6bcc27\",\n  \"total_cost_usd\": 0.029,\n  \"duration_ms\": 2562\n}\n```\n\n### Fields Used by Bridge\n- `result` — the reply text to send back to WhatsApp\n- `session_id` — stable ID to store in sessions.json and pass to next `--resume` call\n- `is_error` — check before sending reply\n\n### New Session (first message from a chat)\n```bash\necho \"user message\" | claude -p --output-format json\n```\nNo `--resume` flag. The returned `session_id` is stored to sessions.json for that chat JID.\n\n### Go Implementation Notes\n- Use `exec.Command` with stdin pipe\n- Set a timeout (e.g. 60s) to avoid hanging on slow responses\n- Run in goroutine to avoid blocking WebSocket handler\n\n### Related\n- #1 Architecture\n","number":4,"repository":"RonenDotan/whatsapp-claude-bridge","title":"Decision: Claude CLI Invocation Format","type":"Issue","url":"https://github.com/RonenDotan/whatsapp-claude-bridge/issues/4"},"id":"PVTI_lAHOAP5YJ84BZFlzzguGTXo","repository":"https://github.com/RonenDotan/whatsapp-claude-bridge","status":"Closed","title":"Decision: Claude CLI Invocation Format"},{"content":{"body":"## Session Persistence Strategy\n\n### Decision: Option C — CLI spawn-on-demand with --resume ✅\n\n### Options Evaluated\n\n**Option A — Anthropic API + stored JSON history**\n- Gateway calls API directly, stores full conversation as JSON array per chat\n- Each call: load history → append message → call API → append reply → save\n- ❌ Requires separate Anthropic API account and billing\n- ✅ Simple, portable\n\n**Option B — Persistent subprocess**\n- Keep one `claude` CLI process alive per active chat\n- Send messages via stdin, read replies from stdout\n- ✅ Slightly lower latency (no process startup)\n- ❌ Process management: what happens on crash? On restart? On idle timeout?\n\n**Option C — Spawn-on-demand + resume ✅ (chosen)**\n- Spawn `claude` process per message, exit after reply\n- Pass `--resume <session_id>` to reload full context from disk\n- Claude Code stores session history locally in `~/.claude/projects/`\n- ✅ No separate API account\n- ✅ No persistent process to babysit\n- ✅ Crash-safe: session survives process restarts\n- ⚠️ ~1-3s extra startup overhead per message (acceptable for WhatsApp cadence)\n\n### Token Cost Note\nAll three options cost the same tokens. LLMs are stateless — full conversation history is sent to Anthropic's servers on every call regardless of whether the local process is alive. See #Prompt Caching ticket for optimization.\n\n### Tested\nTwo-call xaxe test passed ✅\n- Call 1: \"Remember this string: xaxe\" → session_id captured\n- Call 2: --resume <session_id> → \"The string you asked me to remember is xaxe.\" ✓\n\n### Related\n- #1 Architecture\n","number":3,"repository":"RonenDotan/whatsapp-claude-bridge","title":"Decision: Session Persistence Strategy (Option C)","type":"Issue","url":"https://github.com/RonenDotan/whatsapp-claude-bridge/issues/3"},"id":"PVTI_lAHOAP5YJ84BZFlzzguGTYI","repository":"https://github.com/RonenDotan/whatsapp-claude-bridge","status":"Closed","title":"Decision: Session Persistence Strategy (Option C)"},{"content":{"body":"## Inline Handler vs. Separate Gateway\n\n### Decision: Inline in Go bridge ✅\n\nWhen a WhatsApp message arrives, the Claude CLI is spawned directly inside the Go bridge's message handler (in a goroutine). No separate HTTP gateway service.\n\n### Options Evaluated\n\n| Option | Description | Verdict |\n|--------|-------------|---------|\n| Inline in Go | Handle Claude call directly in main.go | ✅ Chosen |\n| Separate gateway | Small Python/Node HTTP server receives webhook from bridge, calls Claude, replies | Deferred |\n\n### Rationale\n- Fewer moving parts, no extra process to manage\n- Go handles goroutines cleanly — spawning a subprocess per message is non-blocking\n- The bridge already has all context: sender JID, message content, HTTP send API\n\n### When to Revisit\nExtract to a standalone gateway when:\n- Adding Telegram (to avoid duplicating session logic in two bridges)\n- Routing to multiple backends (Claude, Codex, etc.) based on message content\n- Needing independent restarts of AI logic without dropping the WhatsApp WebSocket connection\n\n### Related\n- #1 Architecture\n","number":2,"repository":"RonenDotan/whatsapp-claude-bridge","title":"Decision: Inline Handler vs. Gateway","type":"Issue","url":"https://github.com/RonenDotan/whatsapp-claude-bridge/issues/2"},"id":"PVTI_lAHOAP5YJ84BZFlzzguGTYk","repository":"https://github.com/RonenDotan/whatsapp-claude-bridge","status":"Closed","title":"Decision: Inline Handler vs. Gateway"},{"content":{"body":"## Architecture Diagrams\n\n### WhatsApp (Current)\n\n```\nUser Phone --> WhatsApp Servers\n                    |\n               WebSocket (persistent)\n                    |\n           whatsmeow (Go lib, embedded in bridge)\n                    |\n            handleMessage()\n             /             \\\n  Claude channel          Codex channel\n       |                       |\nclaude -p --resume         codex exec resume\n       |                       |\n JSON response            text output\n       |                       |\nsendWhatsAppMessage()   sendWhatsAppMessage()\n```\n\n### Signal (Proposed)\n\n```\nUser Phone --> Signal Servers\n                    |\n             Signal Protocol\n                    |\n         signal-cli (Java daemon)\n                    |\n           JSON-RPC / WebSocket\n                    |\n      signal-cli-rest-api (Go wrapper)\n                    |\n         WebSocket subscription\n                    |\n        handleSignalMessage() [new]\n             /             \\\n  Claude channel          Codex channel\n       |                       |\nclaude -p --resume         codex exec resume\n       |                       |\n JSON response            text output\n       |                       |\nsendSignalMessage() [new]  sendSignalMessage() [new]\n```\n\n### Comparison\n\n| Component | WhatsApp | Signal |\n|---|---|---|\n| Client library | whatsmeow (Go, embedded) | signal-cli (Java) + REST API wrapper (Go) |\n| Connection type | WebSocket, persistent | WebSocket via REST subscription |\n| Phone number | Existing WhatsApp number | Separate Signal-registered number required |\n| Message handler | handleMessage() | handleSignalMessage() (new) |\n| LLM routing | claude / codex CLI | Same (unchanged) |\n| Complexity | Single binary | Two processes (signal-cli + bridge) |\n\n---\n\n## Message Flow Diagrams\n\n### WhatsApp (Current)\n\n```\nUser's Phone\n     |\n     |  WhatsApp protocol (end-to-end encrypted)\n     v\nWhatsApp Servers\n     |\n     |  WebSocket (persistent connection)\n     v\n┌─────────────────────────────────────────────┐\n│  whatsmeow  (Go library, inside main.go)    │\n│  Handles protocol, encryption, auth         │\n└──────────────────┬──────────────────────────┘\n                   |\n                   |  Go function call (in-process)\n                   v\n┌─────────────────────────────────────────────┐\n│           handleMessage()  in main.go       │\n│                                             │\n│  1. Is it a message we sent? → ignore       │\n│  2. \"clear session\" → delete session file   │\n│  3. Stats query → return cached stats       │\n│  4. Loop detected? → return warning         │\n│  5. Allowed chat?                           │\n│       ├── Codex group ──→ handleWithCodex() │\n│       └── Other group ──→ handleWithClaude()│\n└────────┬──────────────────────┬─────────────┘\n         |                      |\n         v                      v\n  ┌─────────────┐       ┌──────────────┐\n  │  codex exec │       │  claude -p   │\n  │  (CLI call) │       │  (CLI call)  │\n  └──────┬──────┘       └──────┬───────┘\n         |                      |\n         v                      v\n  ┌─────────────┐       ┌──────────────┐\n  │  GPT-5.5    │       │  Claude API  │\n  │  (OpenAI)   │       │  (Anthropic) │\n  └──────┬──────┘       └──────┬───────┘\n         |                      |\n         └──────────┬───────────┘\n                    |\n                    v\n         ┌──────────────────┐\n         │ sendWhatsApp     │\n         │ Message()        │\n         │ via whatsmeow    │\n         └────────┬─────────┘\n                  |\n                  v\n          WhatsApp Servers\n                  |\n                  v\n           User's Phone\n```\n\n---\n\n### Signal (Proposed)\n\n```\nUser's Phone\n     |\n     |  Signal protocol (end-to-end encrypted)\n     v\nSignal Servers\n     |\n     |  Signal protocol\n     v\n┌─────────────────────────────────────────────┐\n│  signal-cli  (Java daemon, separate process)│\n│  Handles protocol, encryption, key storage  │\n└──────────────────┬──────────────────────────┘\n                   |\n                   |  JSON-RPC over WebSocket\n                   v\n┌─────────────────────────────────────────────┐\n│  signal-cli-rest-api  (Go service, :8080)   │\n│  Wraps signal-cli as HTTP + WebSocket API   │\n└──────────────────┬──────────────────────────┘\n                   |\n                   |  WebSocket subscription (to be built)\n                   v\n┌─────────────────────────────────────────────┐\n│       handleSignalMessage()  in main.go     │\n│                                             │\n│  ← SAME routing logic as WhatsApp →        │\n│  clear session / stats / loop / Claude /   │\n│  Codex — all identical                     │\n└──────────────────┬──────────────────────────┘\n                   |\n                   v\n          Claude/Codex CLI\n          (same as WhatsApp)\n                   |\n                   v\n         ┌──────────────────┐\n         │ sendSignalMessage│\n         │ POST /v2/send    │\n         │ to rest-api      │\n         └────────┬─────────┘\n                  |\n                  v\n       signal-cli-rest-api\n                  |\n                  v\n            signal-cli\n                  |\n                  v\n          Signal Servers\n                  |\n                  v\n           User's Phone\n```\n\n---\n\n### Key Difference\n\n| | WhatsApp | Signal |\n|---|---|---|\n| Protocol layer | whatsmeow (embedded Go lib, in-process) | signal-cli (Java daemon, separate process) |\n| How bridge talks to it | Direct function calls | HTTP/WebSocket via signal-cli-rest-api |\n| What we build | Everything in main.go | Add WebSocket listener + REST sender to main.go |\n| AI routing | Claude/Codex CLI | Identical — reuses same handlers |\n\n---\n\n## Overview\nAdd Signal messenger as a second channel alongside WhatsApp, routing messages to Claude/Codex using the same bridge logic.\n\n## Current WhatsApp Architecture (for reference)\n\nThe WhatsApp setup has two layers:\n\n**Layer 1 — Protocol: `whatsmeow`**\nA Go library embedded directly inside `main.go`. Handles the low-level WhatsApp WebSocket protocol (connecting to WhatsApp servers, encryption, message format). Not a separate process — just imported code.\n\n**Layer 2 — Our bridge: `main.go`**\nUses whatsmeow to receive incoming messages, routes them to Claude or Codex CLI, and sends replies back. All session management, loop detection, stats, and commands (clear session, etc.) live here.\n\n## Signal Architecture\n\nSignal requires one extra layer because there is no embeddable Go library for the Signal protocol (unlike whatsmeow for WhatsApp).\n\n**Layer 1 — Protocol: `signal-cli`**\n- Repo: https://github.com/AsamK/signal-cli (4.6k stars, actively maintained, latest release April 2026)\n- Java-based standalone daemon that handles the Signal protocol, encryption, and message transport\n- The Signal equivalent of whatsmeow — but runs as a separate background process instead of being embedded\n- Requires a phone number to register (separate from your WhatsApp number)\n\n**Layer 2 — REST wrapper: `signal-cli-rest-api`**\n- Repo: https://github.com/bbernhard/signal-cli-rest-api (2.5k stars, written in Go, Docker-based)\n- Wraps signal-cli and exposes it as HTTP REST + WebSocket\n- Send messages: `POST /v2/send`\n- Receive messages: WebSocket subscription\n- This replaces the direct whatsmeow function calls — instead of calling library functions, we call HTTP endpoints\n\n**Layer 3 — Bridge additions (to be built)**\nNew code in `main.go` (or a new `signal.go` file) that:\n- Connects to signal-cli-rest-api WebSocket to receive incoming Signal messages\n- Routes messages through the same Claude/Codex/stats/clear-session logic already built for WhatsApp\n- Sends replies via the signal-cli-rest-api REST endpoint\n\n## Summary Comparison\n\n| | WhatsApp | Signal |\n|---|---|---|\n| Protocol layer | whatsmeow (embedded Go lib) | signal-cli (Java daemon) |\n| API layer | built into main.go | signal-cli-rest-api (separate Go service) |\n| Our bridge | main.go | additions to main.go |\n| AI routing | Claude/Codex CLI | same |\n\n## Implementation Steps\n1. Set up signal-cli-rest-api (Docker or native) and register a Signal number\n2. Verify send/receive works via the REST API manually\n3. Add Signal WebSocket listener to the bridge\n4. Add `sendSignalMessage()` helper using the REST endpoint\n5. Route Signal messages through existing Claude/Codex handlers\n6. Add Signal-specific allowed chats whitelist\n7. Test end-to-end\n\n## Notes\n- Signal requires a dedicated phone number (can use a VoIP number)\n- signal-cli must be kept up to date — Signal expires clients after ~3 months\n- The AI routing, session management, loop detection, and all gateway features carry over unchanged\n\n---\n\n## Architecture Discussion & Decisions\n\n### signal-cli role clarified\n`signal-cli` is the Signal equivalent of `whatsmeow` — it handles the Signal protocol and encryption so we don't have to. The key difference is that whatsmeow is a Go library embedded directly in the bridge binary, while signal-cli is a separate Java process. But the role is identical: talk to the servers, handle the protocol, expose messages to our code.\n\n### signal-cli-rest-api is optional\nThe REST API wrapper is not required. `signal-cli` running in daemon mode exposes a WebSocket directly. The Go bridge can subscribe to that WebSocket without the REST layer in between, simplifying the stack to:\n\n```\nSignal servers --> signal-cli (daemon) --> WebSocket (local) --> Go bridge\n```\n\n### Two implementation options\n\n**Option A: Standalone script (simplest, fastest)**\nA thin Python or shell script that:\n1. Calls `signal-cli receive` to get incoming messages\n2. Routes to `claude` or `codex` CLI (same commands as Go bridge)\n3. Calls `signal-cli send` to reply\n\nNo changes to the Go bridge. Signal runs as a completely independent process.\nTradeoff: separate session stores and logic — not shared with WhatsApp channel.\n\n**Option B: Integrate into Go bridge**\nAdd a WebSocket subscriber to the Go bridge that connects to signal-cli daemon and adds a `handleSignalMessage()` handler. Logic (session management, loop detection, stats) is shared across WhatsApp and Signal.\nTradeoff: more implementation work, but unified architecture.\n\n### Decision: do NOT fork signal-cli\nThe idea of modifying signal-cli source (Java, open source) to embed Claude/Codex routing was considered and rejected. Reasons:\n- Maintenance burden: would need to track upstream for security/protocol updates\n- Wrong abstraction layer: business logic doesn't belong in the protocol handler\n- The thin script approach achieves the same result with zero overhead\n\n### Open question\nWhich option to implement: standalone script (A) or Go bridge integration (B)?\nOption A is faster to ship. Option B makes more sense if unified session management across platforms becomes important.\n","number":13,"repository":"RonenDotan/whatsapp-claude-bridge","title":"Signal Integration","type":"Issue","url":"https://github.com/RonenDotan/whatsapp-claude-bridge/issues/13"},"id":"PVTI_lAHOAP5YJ84BZFlzzguKD-Y","repository":"https://github.com/RonenDotan/whatsapp-claude-bridge","status":"Closed","title":"Signal Integration"},{"content":{"body":"First step of install script. Detect and verify: Go, Java 21+, Python 3.x, Node/npm (if Claude selected), signal-cli (download if missing), claude CLI (if Claude selected), codex (if Codex selected). Warn clearly if anything is missing and provide install instructions. Part of #18.","number":24,"repository":"RonenDotan/whatsapp-claude-bridge","title":"feat: Installation wizard - prerequisites check","type":"Issue","url":"https://github.com/RonenDotan/whatsapp-claude-bridge/issues/24"},"id":"PVTI_lAHOAP5YJ84BZFlzzgudZwU","repository":"https://github.com/RonenDotan/whatsapp-claude-bridge","status":"Closed","title":"feat: Installation wizard - prerequisites check"},{"content":{"body":"Idempotent WhatsApp setup step. Check if `store/whatsapp.db` has a valid session — if so, skip. Otherwise start whatsapp-mcp-server and display the QR code for the user to scan with their phone. Confirm successful connection before proceeding. Part of #18.","number":25,"repository":"RonenDotan/whatsapp-claude-bridge","title":"feat: Installation wizard - WhatsApp pairing","type":"Issue","url":"https://github.com/RonenDotan/whatsapp-claude-bridge/issues/25"},"id":"PVTI_lAHOAP5YJ84BZFlzzgudZxc","repository":"https://github.com/RonenDotan/whatsapp-claude-bridge","status":"Closed","title":"feat: Installation wizard - WhatsApp pairing"},{"content":{"body":"Parallel to `start.ps1`, create a `start.sh` that starts/restarts all components on Linux and macOS. Handles `pkill`/`pgrep` for process management, no `.bat`/`.exe` extensions, signal-cli found via PATH or `~/.local/bin`. Supports same args: `signal`, `whatsapp`, `bridge`, or no arg for all. Part of #18.\n\n---\n\n## Implementation Plan\n\n### Step 1 — Scaffold: shebang, strict mode, script-dir detection\n```bash\n#!/usr/bin/env bash\nset -euo pipefail\nSCRIPT_DIR=\"$(cd \"$(dirname \"${BASH_SOURCE[0]}\")\" && pwd)\"\nMCP_DIR=\"$SCRIPT_DIR/../whatsapp-mcp-server\"\n```\nUse `$BASH_SOURCE` (more reliable than `$0` when sourced). Avoid `realpath` — missing on macOS without coreutils.\n\n### Step 2 — Argument parsing + help text\nMirror `start.ps1` exactly: first positional arg defaults to `all`; `--help`, `-h`, `help` all print help. Same wording, but reference `./start.sh` instead of `start.bat`.\n\n### Step 3 — Signal-cli discovery function (`find_signal_cli`)\nSearch order:\n1. `command -v signal-cli` — installed on PATH (Homebrew, apt, manual)\n2. `~/.local/bin/signal-cli` — common Linux manual install\n3. Newest `/tmp/signal-cli-*-extracted/bin/signal-cli` — mirrors Windows TEMP convention\n4. macOS extras: `/usr/local/bin/signal-cli`, `/opt/homebrew/bin/signal-cli`\n5. Also scan `$TMPDIR` (macOS temp is `/var/folders/...`, not `/tmp`)\n\n**Fix vs PS1 bug**: PS1 runs signal-cli discovery unconditionally at startup, so `start.bat bridge` fails if signal-cli is absent. The `.sh` should only call `find_signal_cli` when the component is `signal` or `all`.\n\n### Step 4 — `kill_by_pattern()` helper\n```bash\nkill_by_pattern() { pkill -f \"$1\" 2>/dev/null || true; }\n```\nEquivalent of PS1's `Kill-ByCommandLine`. The `|| true` prevents `set -e` from aborting when no matching process exists. Works identically on Linux and macOS.\n\n### Step 5 — `restart_signal_cli()`\n- `kill_by_pattern signal-cli`\n- `sleep 1` — PS1 uses 800ms; BSD `sleep` on macOS accepts only integers, so round up to 1s\n- Launch: `nohup \"$SIGNAL_CLI_PATH\" daemon --tcp 0.0.0.0:7583 >/dev/null 2>&1 &`\n- Print PID with `[OK] signal-cli started (PID $!)`\n\n### Step 6 — `restart_whatsapp_mcp()`\n- Kill python processes matching `main.py`\n- `sleep 1`\n- Launch from `$MCP_DIR` using `.venv/bin/python main.py` (Unix venv path vs PS1's `.venv\\Scripts\\python.exe`)\n- Background with stdout/stderr discarded (PS1 uses `-WindowStyle Hidden`)\n- Print PID\n\n### Step 7 — `restart_bridge()` with whatsmeow update check\nPort the PS1 update-check logic using `curl` + `python3` for JSON (avoids `jq` dependency; Python3 is already a prerequisite):\n```bash\nlatest=$(curl -sf 'https://proxy.golang.org/go.mau.fi/whatsmeow/@latest' \\\n    | python3 -c \"import json,sys; print(json.load(sys.stdin)['Version'])\")\ncurrent=$(grep 'go.mau.fi/whatsmeow' \"$SCRIPT_DIR/go.mod\" | awk '{print $2}')\n```\nIf versions differ: `go get go.mau.fi/whatsmeow@latest && go build -o whatsapp-bridge .` (no `.exe`).\n\nLaunch with append-mode log redirection:\n```bash\nnohup \"$SCRIPT_DIR/whatsapp-bridge\" >> \"$SCRIPT_DIR/bridge.log\" 2>> \"$SCRIPT_DIR/bridge.err\" &\n```\n\n### Step 8 — Main dispatch (`case` statement)\n```bash\ncase \"$component\" in\n  signal)   restart_signal_cli ;;\n  whatsapp) restart_whatsapp_mcp ;;\n  bridge)   restart_bridge ;;\n  all)      restart_signal_cli; restart_whatsapp_mcp; restart_bridge ;;\n  *)        echo \"Unknown component: $component\"; echo \"Run: ./start.sh --help\"; exit 1 ;;\nesac\n```\n\n### Step 9 — Make executable (with git)\n```bash\nchmod +x start.sh\ngit update-index --chmod=+x start.sh\n```\nGit tracks the executable bit; setting it via `update-index` ensures it's preserved on Windows checkouts too.\n\n### Step 10 — Update `.claude/CLAUDE.md`\nAdd `start.sh` to the \"Correct commands\" block alongside `start.bat`/`start.ps1`:\n```\n- ./start.sh           (Linux/macOS: restart all components)\n- ./start.sh bridge    (Linux/macOS: restart Go bridge only)\n```\n\n---\n\n## Edge Cases\n\n| Issue | Handling |\n|---|---|\n| `sleep 0.8` doesn't work on macOS BSD `sleep` | Use `sleep 1` throughout |\n| `pkill` returns exit 1 if nothing matched | `\\|\\| true` absorbs it under `set -e` |\n| `curl` absent | Wrap update check in `if command -v curl` with warn-and-skip fallback, mirroring PS1's `catch` |\n| Bridge binary not yet built | Add `-f` existence check before launching; run `go build` if missing |\n| macOS TMPDIR is not `/tmp` | Scan `$TMPDIR` in addition to `/tmp` for signal-cli-*-extracted dirs |\n\n---\n\n## Complexity: Low — 1–1.5 hours\nDirect port of existing PS1 logic. No new architecture. Main non-trivial decisions are signal-cli path discovery and macOS/Linux `sleep`/`pkill` compatibility.","number":23,"repository":"RonenDotan/whatsapp-claude-bridge","title":"feat: Create start.sh for Linux/macOS","type":"Issue","url":"https://github.com/RonenDotan/whatsapp-claude-bridge/issues/23"},"id":"PVTI_lAHOAP5YJ84BZFlzzgudZvk","repository":"https://github.com/RonenDotan/whatsapp-claude-bridge","status":"Closed","title":"feat: Create start.sh for Linux/macOS"},{"content":{"body":"Two fixes needed: (1) `signalAttachmentsDir` (line 73) is hardcoded to Linux XDG path with no macOS or Windows fallback. (2) `detectSignalOwnerNumber` (line 248) is missing the macOS `~/Library/Application Support/signal-cli/...` candidate. Part of #18.","number":22,"repository":"RonenDotan/whatsapp-claude-bridge","title":"fix: signal.go cross-platform paths (macOS + Windows attachments dir)","type":"Issue","url":"https://github.com/RonenDotan/whatsapp-claude-bridge/issues/22"},"id":"PVTI_lAHOAP5YJ84BZFlzzgudZu0","repository":"https://github.com/RonenDotan/whatsapp-claude-bridge","status":"Closed","title":"fix: signal.go cross-platform paths (macOS + Windows attachments dir)"},{"content":{"body":"Ask user: Claude, Codex, or both. Prompt for and securely store only the required API keys. If Claude selected: verify `claude auth`, write `bypassPermissions` to `.claude/settings.local.json`, set default LLM for new chats. If Codex selected: verify/store OpenAI API key. Part of #18.","number":27,"repository":"RonenDotan/whatsapp-claude-bridge","title":"feat: Installation wizard - LLM configuration","type":"Issue","url":"https://github.com/RonenDotan/whatsapp-claude-bridge/issues/27"},"id":"PVTI_lAHOAP5YJ84BZFlzzgudZzw","repository":"https://github.com/RonenDotan/whatsapp-claude-bridge","status":"Closed","title":"feat: Installation wizard - LLM configuration"},{"content":{"body":"start.ps1 currently has hardcoded `C:\\Users\\ronen\\whatsapp-mcp\\...` paths. Should derive all paths relative to the script's own location so it works on any machine. Part of #18.","number":19,"repository":"RonenDotan/whatsapp-claude-bridge","title":"fix: Make start.ps1 paths relative (not hardcoded to C:\\Users\\ronen)","type":"Issue","url":"https://github.com/RonenDotan/whatsapp-claude-bridge/issues/19"},"id":"PVTI_lAHOAP5YJ84BZFlzzgudZrY","repository":"https://github.com/RonenDotan/whatsapp-claude-bridge","status":"Closed","title":"fix: Make start.ps1 paths relative (not hardcoded to C:\\Users\\ronen)"},{"content":{"body":"After install completes: start all components via the startup script, send a test ping to verify bridge is responding, print a clear summary of what's running and how to activate the first chat (`!meet-claude` or `!meet-codex`). Part of #18.","number":28,"repository":"RonenDotan/whatsapp-claude-bridge","title":"feat: Installation wizard - final verification & summary","type":"Issue","url":"https://github.com/RonenDotan/whatsapp-claude-bridge/issues/28"},"id":"PVTI_lAHOAP5YJ84BZFlzzgudZ0c","repository":"https://github.com/RonenDotan/whatsapp-claude-bridge","status":"Closed","title":"feat: Installation wizard - final verification & summary"},{"content":{"body":"Idempotent Signal setup step. Check if signal-cli accounts.json exists — if so, skip. Otherwise run `signal-cli link --name \"AI-Bridge\"`, generate a QR code from the `sgnl://` URI, display it for the user to scan in their Signal app. Confirm link before proceeding. Part of #18.","number":26,"repository":"RonenDotan/whatsapp-claude-bridge","title":"feat: Installation wizard - Signal linking","type":"Issue","url":"https://github.com/RonenDotan/whatsapp-claude-bridge/issues/26"},"id":"PVTI_lAHOAP5YJ84BZFlzzgudZyQ","repository":"https://github.com/RonenDotan/whatsapp-claude-bridge","status":"Closed","title":"feat: Installation wizard - Signal linking"},{"content":{"body":"On each startup, check if a newer whatsmeow version is available via Go module proxy. If so, run `go get -u go.mau.fi/whatsmeow && go build` before starting the bridge. Print update notice when a new version is found. Part of #18.","number":21,"repository":"RonenDotan/whatsapp-claude-bridge","title":"feat: Add whatsmeow auto-update check to startup script","type":"Issue","url":"https://github.com/RonenDotan/whatsapp-claude-bridge/issues/21"},"id":"PVTI_lAHOAP5YJ84BZFlzzgudZtc","repository":"https://github.com/RonenDotan/whatsapp-claude-bridge","status":"Closed","title":"feat: Add whatsmeow auto-update check to startup script"},{"content":{"body":"Bridge runs with `-WindowStyle Hidden` — no way to see errors. Redirect stdout/stderr of all three components to log files (`bridge.log`, `whatsapp-mcp.log`, `signal-cli.log`) in the bridge directory. Part of #18.","number":20,"repository":"RonenDotan/whatsapp-claude-bridge","title":"feat: Add logging to startup script (bridge.log)","type":"Issue","url":"https://github.com/RonenDotan/whatsapp-claude-bridge/issues/20"},"id":"PVTI_lAHOAP5YJ84BZFlzzgudZsc","repository":"https://github.com/RonenDotan/whatsapp-claude-bridge","status":"Closed","title":"feat: Add logging to startup script (bridge.log)"},{"content":{"body":"## Speak Back\n\nText-to-speech: Claude responds with a voice/audio message instead of (or in addition to) text.\n\n_To be defined._\n","number":10,"repository":"RonenDotan/whatsapp-claude-bridge","title":"Speak Back","type":"Issue","url":"https://github.com/RonenDotan/whatsapp-claude-bridge/issues/10"},"id":"PVTI_lAHOAP5YJ84BZFlzzguGTZI","repository":"https://github.com/RonenDotan/whatsapp-claude-bridge","status":"Open","title":"Speak Back"},{"content":{"body":"## Overview\nAdd health check monitoring for the WhatsApp bridge gateway to detect errors and optionally restart the bridge automatically.\n\n## Options Discussed\n\n### Option A: Built-in Gateway Health Check (Preferred)\nAdd a Go goroutine inside the bridge itself that runs on a ticker (e.g. every 5 minutes). It checks internal state and sends a WhatsApp alert message to a designated chat if something is wrong. No external scheduler needed, zero additional cost, lives entirely inside the process.\n\nWhat it could check:\n- Last message received timestamp (is the bridge still getting events?)\n- Error counters (how many `⚠️` errors in the last N minutes)\n- WhatsApp connection state (is the client still connected?)\n\n### Option B: Windows Task Scheduler Script\nA PowerShell script run by Windows Task Scheduler on a cron-like schedule. It checks the bridge process is running, greps bridge.log/bridge.err for recent errors, and if issues are found, sends a WhatsApp message via the bridge's REST API (POST to localhost:8080/send). Zero LLM cost, completely deterministic.\n\n### Option C: Cowork Scheduled Task\nA Claude Code scheduled task that fires on a schedule and uses the `whatsapp-bridge-monitor` skill to check the bridge. Uses LLM tokens on each run (~few hundred tokens per check). Most flexible but has ongoing cost. Best suited for infrequent checks (every 30-60 min) rather than continuous monitoring.\n\n## Decision Points\n- How often to check (5 min, 15 min, 30 min)\n- Alert-only vs auto-restart on failure\n- Which chat receives the health alerts (personal chat or a dedicated group)\n- Whether to combine: gateway goroutine for fast error detection + scheduled task for deeper analysis\n\n## Notes\n- The bridge already has `bridge.log` and `bridge.err` log files\n- The REST API on :8080 can be used to send WhatsApp messages from external scripts\n- A `whatsapp-bridge-monitor` skill already exists for manual triggering\n- The gateway option (A) is most elegant: self-contained, always running, zero extra infra","number":11,"repository":"RonenDotan/whatsapp-claude-bridge","title":"Health Check Monitoring","type":"Issue","url":"https://github.com/RonenDotan/whatsapp-claude-bridge/issues/11"},"id":"PVTI_lAHOAP5YJ84BZFlzzguI2PY","repository":"https://github.com/RonenDotan/whatsapp-claude-bridge","title":"Health Check Monitoring"},{"content":{"body":"Each chat (WhatsApp contact, WhatsApp group, Signal contact, Signal group) should get its own dedicated working directory on disk. When the bridge invokes Claude CLI for a chat, it sets the working directory to that chat's folder. Benefits:\n- Claude CLI reads CLAUDE.md from the chat dir, not the project dir — clean separation between coding context and chat context\n- Each chat's session history, context files, and personality prompt can live in its folder\n- Can be used to set per-chat personality: place a CLAUDE.md in the chat dir with the personality instructions, rather than passing a system prompt flag\n- Fully manageable and inspectable: each chat is a self-contained folder\n\nImplementation notes:\n- Chat dirs stored under e.g. `store/chats/<sanitized-chat-id>/`\n- Each dir contains: `CLAUDE.md` (personality/context), session state\n- Claude CLI invoked with working directory set to the chat's folder\n- Default CLAUDE.md for new chats: minimal clean assistant persona","number":14,"repository":"RonenDotan/whatsapp-claude-bridge","title":"feat: per-chat isolated working directories for Claude sessions","type":"Issue","url":"https://github.com/RonenDotan/whatsapp-claude-bridge/issues/14"},"id":"PVTI_lAHOAP5YJ84BZFlzzguSEgQ","repository":"https://github.com/RonenDotan/whatsapp-claude-bridge","title":"feat: per-chat isolated working directories for Claude sessions"},{"content":{"body":"Currently the bridge handles text messages and audio (voice notes via Whisper). Add support for:\n- Images: download and pass to Claude as vision input (Claude supports image attachments via CLI)\n- Files/documents: download, detect type, pass to Claude where supported\n- Both WhatsApp (via whatsmeow media download) and Signal (via signal-cli attachment download) should be supported\n- Graceful fallback if file type is unsupported","number":15,"repository":"RonenDotan/whatsapp-claude-bridge","title":"feat: support images and file attachments in WhatsApp and Signal chats","type":"Issue","url":"https://github.com/RonenDotan/whatsapp-claude-bridge/issues/15"},"id":"PVTI_lAHOAP5YJ84BZFlzzguSEgo","repository":"https://github.com/RonenDotan/whatsapp-claude-bridge","title":"feat: support images and file attachments in WhatsApp and Signal chats"},{"content":{"body":"Current transcription uses local `whisper` CLI (openai-whisper Python package) which is slow, requires GPU/CPU resources locally, and has a 5-minute timeout. Replace with OpenAI Whisper API:\n- Faster, more accurate, no local model required\n- Requires OPENAI_API_KEY env var\n- Send audio file as multipart/form-data to api.openai.com/v1/audio/transcriptions\n- Keep local whisper as fallback if API key not set\n- Update settings.json to include `whisper_api_enabled: true/false`","number":16,"repository":"RonenDotan/whatsapp-claude-bridge","title":"feat: replace local Whisper CLI with OpenAI Whisper API","type":"Issue","url":"https://github.com/RonenDotan/whatsapp-claude-bridge/issues/16"},"id":"PVTI_lAHOAP5YJ84BZFlzzguSEgw","repository":"https://github.com/RonenDotan/whatsapp-claude-bridge","title":"feat: replace local Whisper CLI with OpenAI Whisper API"},{"content":{"body":"The bridge has been refactored into a modular structure. CLAUDE.md needs to be updated to reflect the new architecture so that coding sessions (Claude Code) have accurate context. Before updating CLAUDE.md, create a proposed draft for review.\n\nCurrent architecture (post-refactor on `refactor` branch):\n- `main.go` — thin orchestrator (~18 lines): loads settings, starts enabled channels\n- `settings.go` — Settings struct, loadSettings() reading store/settings.json\n- `shared.go` — all shared logic: session CRUD (sessions.json), Claude/Codex routing (handleWithClaude/handleWithCodex with sendFn closures), Whisper transcription, whitelist management, personality loading, loop detection, stats\n- `whatsapp.go` — all WhatsApp-specific: whatsmeow client, event handler, message handler, bridge command handler, REST server, startWhatsApp()\n- `signal.go` — all Signal-specific: TCP connection to signal-cli daemon on :7583, JSON-RPC message handling, signal command handler, startSignalListener()\n- `personalities.go` — personality preset definitions (default/kids/pro/creative)\n- `store/settings.json` — channel enable flags: whatsapp_enabled, signal_enabled\n- `store/allowed_chats.json` / `store/codex_allowed_chats.json` — WhatsApp whitelists\n- `store/signal_allowed_chats.json` / `store/signal_codex_allowed_chats.json` — Signal whitelists\n- `store/chat_personalities.json` — per-chat personality assignments\n- `store/sessions.json` / `store/codex_sessions.json` — Claude/Codex session IDs per chat\n\nBuild: `go build -o whatsapp-bridge.exe .` from the whatsapp-bridge directory\nRestart: stop whatsapp-bridge.exe process, start new one\nsignal-cli daemon must be running separately on :7583\n\nThis issue tracks: (1) writing the proposed CLAUDE.md draft, (2) getting approval, (3) updating the file","number":17,"repository":"RonenDotan/whatsapp-claude-bridge","title":"docs: document new refactored architecture in CLAUDE.md for coding session context","type":"Issue","url":"https://github.com/RonenDotan/whatsapp-claude-bridge/issues/17"},"id":"PVTI_lAHOAP5YJ84BZFlzzguSEg4","repository":"https://github.com/RonenDotan/whatsapp-claude-bridge","title":"docs: document new refactored architecture in CLAUDE.md for coding session context"},{"content":{"body":"## Overview\n\nInteractive installation and startup scripts that guide a new user from zero to a fully running WhatsApp/Signal AI bridge. Covers channel selection, prerequisite checks, device pairing, LLM configuration, and startup automation.\n\n## Architecture & Key Decisions\n\n### What gets installed\n- **signal-cli** — Java daemon that connects to Signal as a linked device (like Signal Desktop). Auto-downloaded if not found.\n- **whatsapp-mcp-server** — Python FastMCP server using whatsmeow to connect WhatsApp as a linked device.  \n- **Go bridge** — the main gateway that routes messages between Signal/WhatsApp and the AI (Claude/Codex).\n\n### Channel pairing model\nBoth WhatsApp and Signal work as independent linked devices — not piggyback on existing apps. Each requires a one-time QR scan with the user's phone. After pairing, credentials are stored locally and connection is fully headless.\n\n- WhatsApp: QR scan via `whatsapp-mcp-server`. Session stored in `store/whatsapp.db`. Idempotent — skips if already paired.\n- Signal: `signal-cli link` command generates a QR. Session stored in signal-cli data dir. Idempotent — skips if already registered.\n\n### LLM selection\nUser chooses Claude, Codex, or both during install. Only the relevant credentials are requested. Each chat is individually activated via `!meet-claude` or `!meet-codex` — no pre-configuration of chats needed.\n\n### Privacy model\nFor non-activated chats, the bridge only reads the first 2 characters of each message to check for `!meet-*` commands. Message content is never stored, logged, or sent to any AI unless the chat has been explicitly activated. WhatsApp chat metadata (JID, name, timestamp) is stored in SQLite regardless.\n\n### whatsmeow auto-update\nOn every startup, `start.ps1`/`start.sh` checks whether a newer whatsmeow version is available. If so, it runs `go get -u go.mau.fi/whatsmeow && go build` before starting the bridge. Most restarts are instant; the occasional one takes a few minutes when WhatsApp releases a protocol update.\n\n### Cross-platform\nThe Go bridge is already cross-platform (uses `filepath.Join` throughout). Two small fixes needed in `signal.go` for macOS paths. Scripts need a `start.sh` parallel to the existing `start.ps1`.\n\n## Installation Flow\n\n```\n┌─────────────────────────────────────────────────────────┐\n│                    install script                        │\n└──────────────────────┬──────────────────────────────────┘\n                       │\n                       ▼\n         ┌─────────────────────────┐\n         │  Check prerequisites    │\n         │  Go / Java / Python /   │\n         │  Node (per selection)   │\n         └────────────┬────────────┘\n                      │\n                      ▼\n         ┌─────────────────────────┐\n         │  Select channels        │\n         │  [1] WhatsApp           │\n         │  [2] Signal             │\n         │  [3] Both               │\n         └────────────┬────────────┘\n                      │\n          ┌───────────┴───────────┐\n          │                       │\n          ▼                       ▼\n  ┌──────────────┐       ┌──────────────┐\n  │  WhatsApp    │       │   Signal     │\n  │  setup       │       │   setup      │\n  │              │       │              │\n  │  Already     │       │  Already     │\n  │  paired? ──Yes─►skip │  linked? ──Yes►skip│\n  │     │No      │       │    │No       │\n  │     ▼        │       │    ▼         │\n  │  Show QR     │       │  Show QR     │\n  │  (scan with  │       │  (scan with  │\n  │  WhatsApp)   │       │  Signal app) │\n  └──────┬───────┘       └──────┬───────┘\n         └───────────┬──────────┘\n                     │\n                     ▼\n         ┌─────────────────────────┐\n         │  Select LLM             │\n         │  [1] Claude             │\n         │  [2] Codex              │\n         │  [3] Both               │\n         └────────────┬────────────┘\n                      │\n                      ▼\n         ┌─────────────────────────┐\n         │  Configure credentials  │\n         │  (only for selected)    │\n         │  • Anthropic API key    │\n         │  • OpenAI API key       │\n         │  • bypassPermissions    │\n         └────────────┬────────────┘\n                      │\n                      ▼\n         ┌─────────────────────────┐\n         │  Build bridge           │\n         │  go build               │\n         └────────────┬────────────┘\n                      │\n                      ▼\n         ┌─────────────────────────┐\n         │  Start all components   │\n         │  (via start script)     │\n         └────────────┬────────────┘\n                      │\n                      ▼\n         ┌─────────────────────────┐\n         │  Verify & summarize     │\n         │  Print: what's running, │\n         │  how to activate chats  │\n         └─────────────────────────┘\n```\n\n## Sub-issues\nSee linked issues below.","number":18,"repository":"RonenDotan/whatsapp-claude-bridge","title":"feat: Installation & Setup Wizard","type":"Issue","url":"https://github.com/RonenDotan/whatsapp-claude-bridge/issues/18"},"id":"PVTI_lAHOAP5YJ84BZFlzzgudZqY","repository":"https://github.com/RonenDotan/whatsapp-claude-bridge","title":"feat: Installation & Setup Wizard"},{"content":{"body":"When a new chat session is created via !meet-claude or !meet-codex, the bridge should copy a template .claude/settings.local.json into a .claude/ subdirectory inside the chat dir (alongside the existing CLAUDE.md copy). This gives each session its own isolated settings file.\n\n**Why:**\n- Claude Code walks up the directory tree from CWD — a settings file in the chat dir takes precedence over the bridge root .claude/settings.local.json\n- Per-session isolation: settings changes in one chat don't affect others\n- Clearing a session resets settings back to the template\n\n**Implementation:**\n- Add a template file at .claude/templates/settings.local.json (or similar) in the codebase — pre-existing, not created by installer\n- In ensureChatDir (shared.go), after creating the chat dir and copying CLAUDE.md, also create a .claude/ subdir and copy the template settings.local.json there\n- The installer (install.ps1) should NOT write .claude/settings.local.json — that's the bridge's responsibility\n\n**Note:** Need to clarify what \"clear session\" does — if it only drops the session ID without deleting the chat dir, the settings file would persist across clears. May need to decide if clearing should also reset settings.","number":29,"repository":"RonenDotan/whatsapp-claude-bridge","title":"feat: per-session .claude/settings.local.json template for Claude/Codex chats","type":"Issue","url":"https://github.com/RonenDotan/whatsapp-claude-bridge/issues/29"},"id":"PVTI_lAHOAP5YJ84BZFlzzgu5UEo","repository":"https://github.com/RonenDotan/whatsapp-claude-bridge","title":"feat: per-session .claude/settings.local.json template for Claude/Codex chats"},{"content":{"body":"## Problem^^bridge.log accumulates indefinitely across all restarts with no truncation or rotation. The same applies to signal-cli.log. On always-on systems these files grow without bound.^^## Options^^**Option 1: Rotate on startup (start.ps1 / start.bat)**^^Before launching the bridge, rename existing logs:^^- bridge.log -> bridge.log.1, bridge.log.1 -> bridge.log.2, etc.^^- Keep last N copies (e.g. 5), delete anything older^^- Start fresh with an empty bridge.log^^**Option 2: Size-based rotation**^^Rotate only when file exceeds a threshold (e.g. 10 MB). Same rename-and-keep-N logic.^^**Option 3: Native Go log rotation (bridge.log only)**^^Use a library like lumberjack inside the bridge process. signal-cli.log would still need script-level handling.^^## Expected behavior^^Log files should be capped at a reasonable size or rotated on each restart.","number":30,"repository":"RonenDotan/whatsapp-claude-bridge","title":"Add log rotation for bridge.log and signal-cli.log","type":"Issue","url":"https://github.com/RonenDotan/whatsapp-claude-bridge/issues/30"},"id":"PVTI_lAHOAP5YJ84BZFlzzgu5UFU","labels":["enhancement"],"repository":"https://github.com/RonenDotan/whatsapp-claude-bridge","title":"Add log rotation for bridge.log and signal-cli.log"}],"totalCount":29}
