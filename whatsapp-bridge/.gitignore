# Compiled binaries
*.exe
*.exe~

# Logs (current + rotated copies)
*.log
*.err
bridge.log.*
bridge.err.*
signal-cli.log.*

# Runtime store (databases only — all other user data lives in bridge-data/ outside the repo)
store/

# Scratch / temp files written by Claude during dev sessions
_run.bat
_run_out.txt
_issue_body.txt
build_*.log
log.Printf
